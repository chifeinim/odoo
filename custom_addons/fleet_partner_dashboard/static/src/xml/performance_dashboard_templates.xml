<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
  <t t-name="owl.OwlPerformanceDashboard" owl="1">
    <div
      t-key="'owl_performance_dashboard_root'"
      class="o_fleet_dashboard"
      style="height: calc(100vh - 70px); overflow-y: auto; padding: 1rem;"
    >
      <t t-if="state.loading">
        <div>Loading…</div>
      </t>
      <t t-else="">
        <h1 class="mb-4">Performance Dashboard</h1>

        <!-- FILTER BAR & TOGGLE -->
        <div class="mb-4 p-3 bg-light rounded d-flex align-items-center gap-2">
          <!-- Anda Product -->
          <div class="dropdown" t-att-class="{'show': state.showProducts}">
            <button class="btn btn-primary dropdown-toggle"
                    type="button"
                    t-on-click="() => toggleDropdown('showProducts')">
              Anda Product
              <t t-if="state.selectedProducts.length">
                (<t t-esc="state.selectedProducts.length"/>)
              </t>
            </button>
            <div class="dropdown-menu"
                 t-att-class="{'show': state.showProducts}"
                 style="max-height:200px;overflow:auto">
              <t t-foreach="state.productTypes" t-as="pt" t-key="pt.id">
                <button class="dropdown-item"
                        t-att-class="{'active': state.selectedProducts.includes(pt.id)}"
                        t-on-click="() => toggleFilter('selectedProducts', pt.id)">
                  <t t-esc="pt.name"/>
                </button>
              </t>
            </div>
          </div>

          <!-- Driver Quality -->
          <div class="dropdown" t-att-class="{'show': state.showScores}">
            <button class="btn btn-primary dropdown-toggle"
                    type="button"
                    t-on-click="() => toggleDropdown('showScores')">
              Driver Quality
              <t t-if="state.selectedScores.length">
                (<t t-esc="state.selectedScores.length"/>)
              </t>
            </button>
            <div class="dropdown-menu" t-att-class="{'show': state.showScores}">
              <t t-foreach="state.qualityScores" t-as="sc" t-key="sc.key">
                <button class="dropdown-item"
                        t-att-class="{'active': state.selectedScores.includes(sc.key)}"
                        t-on-click="() => toggleFilter('selectedScores', sc.key)">
                  <t t-esc="sc.label"/>
                </button>
              </t>
            </div>
          </div>

          <!-- Driver Category -->
          <div class="dropdown" t-att-class="{'show': state.showCategories}">
            <button class="btn btn-primary dropdown-toggle"
                    type="button"
                    t-on-click="() => toggleDropdown('showCategories')">
              Driver Category
              <t t-if="state.selectedCategories.length">
                (<t t-esc="state.selectedCategories.length"/>)
              </t>
            </button>
            <div class="dropdown-menu" t-att-class="{'show': state.showCategories}">
              <t t-foreach="state.categories" t-as="ct" t-key="ct">
                <button class="dropdown-item"
                        t-att-class="{'active': state.selectedCategories.includes(ct)}"
                        t-on-click="() => toggleFilter('selectedCategories', ct)">
                  <t t-esc="ct"/>
                </button>
              </t>
            </div>
          </div>

          <!-- Time Period -->
          <div class="dropdown" t-att-class="{'show': state.showPeriod}">
            <button class="btn btn-primary dropdown-toggle"
                    type="button"
                    t-on-click="() => toggleDropdown('showPeriod')">
              <t t-esc="state.selectedPeriod"/>
            </button>
            <div class="dropdown-menu" t-att-class="{'show': state.showPeriod}">
              <t t-foreach="state.timePeriods" t-as="pw" t-key="pw">
                <button class="dropdown-item"
                        t-att-class="{'active': state.selectedPeriod === pw}"
                        t-on-click="() => changePeriod(pw)">
                  <t t-esc="pw"/>
                </button>
              </t>
            </div>
          </div>

          <!-- Explicit Dates -->
          <div class="d-flex align-items-center gap-1">
            <label class="mb-0">From:</label>
            <input type="date" class="form-control"
                   t-att-value="state.startDate"
                   t-on-change="onStartDateChange"/>
          </div>
          <div class="d-flex align-items-center gap-1">
            <label class="mb-0">To:</label>
            <input type="date" class="form-control"
                   t-att-value="state.endDate"
                   t-on-change="onEndDateChange"/>
          </div>

          <!-- Toggle Cards/Charts -->
          <button class="btn btn-primary ms-auto"
                  t-on-click="toggleView">
            <t t-esc="state.showCharts ? 'Show Cards' : 'Show Charts'"/>
          </button>
        </div>

        <!-- KPI CARDS: always 3 per row -->
        <t t-if="!state.showCharts">
          <!-- first row: Active, Trips, Supply -->
          <div class="row mb-4">
            <!-- Active Drivers -->
            <div class="col-md-4 mb-3">
              <div class="card text-center h-100">
                <div class="card-body">
                  <h5 class="card-title">Active Drivers</h5>
                  <h2><t t-esc="formatNumber(state.metrics.activeDrivers, 0)"/></h2>
                  <p class="card-text">
                    <t t-set="prev"  t-value="state.metrics.prevActiveDrivers"/>
                    <t t-set="curr"  t-value="state.metrics.activeDrivers"/>
                    <t t-set="delta" t-value="prev ? ((curr - prev)/prev*100) : 0"/>
                    <t t-if="delta >= 0">
                      <span class="text-success">
                        &#9650; <t t-esc="delta.toFixed(1)"/>%
                      </span>
                    </t>
                    <t t-else="">
                      <span class="text-danger">
                        &#9660; <t t-esc="Math.abs(delta).toFixed(1)"/>%
                      </span>
                    </t>
                    &#160;from last period
                  </p>
                </div>
              </div>
            </div>
            <!-- Total Trips -->
            <div class="col-md-4 mb-3">
              <div class="card text-center h-100">
                <div class="card-body">
                  <h5 class="card-title">Total Trips</h5>
                  <h2><t t-esc="formatNumber(state.metrics.tripCount, 0)"/></h2>
                  <p class="card-text">
                    <t t-set="prev"  t-value="state.metrics.prevTripCount"/>
                    <t t-set="curr"  t-value="state.metrics.tripCount"/>
                    <t t-set="delta" t-value="prev ? ((curr - prev)/prev*100) : 0"/>
                    <t t-if="delta >= 0">
                      <span class="text-success">
                        &#9650; <t t-esc="delta.toFixed(1)"/>%
                      </span>
                    </t>
                    <t t-else="">
                      <span class="text-danger">
                        &#9660; <t t-esc="Math.abs(delta).toFixed(1)"/>%
                      </span>
                    </t>
                    &#160;from last period
                  </p>
                </div>
              </div>
            </div>
            <!-- Supply Hours -->
            <div class="col-md-4 mb-3">
              <div class="card text-center h-100">
                <div class="card-body">
                  <h5 class="card-title">Supply Hours</h5>
                  <h2><t t-esc="formatNumber(state.metrics.supplyHours, 1)"/>h</h2>
                  <p class="card-text">
                    <t t-set="prev"  t-value="state.metrics.prevSupplyHours"/>
                    <t t-set="curr"  t-value="state.metrics.supplyHours"/>
                    <t t-set="delta" t-value="prev ? ((curr - prev)/prev*100) : 0"/>
                    <t t-if="delta >= 0">
                      <span class="text-success">
                        &#9650; <t t-esc="delta.toFixed(1)"/>%
                      </span>
                    </t>
                    <t t-else="">
                      <span class="text-danger">
                        &#9660; <t t-esc="Math.abs(delta).toFixed(1)"/>%
                      </span>
                    </t>
                    &#160;from last period
                  </p>
                </div>
              </div>
            </div>
          </div>
          <!-- second row: Total Cash Earned -->
          <div class="row mb-4">
            <div class="col-md-4 mb-3">
              <div class="card text-center h-100">
                <div class="card-body">
                  <h5 class="card-title">Cash Earned</h5>
                  <h2>Kz <t t-esc="formatNumber(state.metrics.cashEarned, 0)"/></h2>
                  <p class="card-text">
                    <t t-set="prev"  t-value="state.metrics.prevCashEarned"/>
                    <t t-set="curr"  t-value="state.metrics.cashEarned"/>
                    <t t-set="delta" t-value="prev ? ((curr - prev)/prev*100) : 0"/>
                    <t t-if="delta >= 0">
                      <span class="text-success">
                        &#9650; <t t-esc="delta.toFixed(1)"/>%
                      </span>
                    </t>
                    <t t-else="">
                      <span class="text-danger">
                        &#9660; <t t-esc="Math.abs(delta).toFixed(1)"/>%
                      </span>
                    </t>
                    &#160;from last period
                  </p>
                </div>
              </div>
            </div>
            <!-- Avg Money per Hour card -->
            <div class="col-md-4 mb-3">
              <div class="card text-center h-100">
                <div class="card-body">
                  <h5 class="card-title">Avg Money / Hour</h5>
                  <h2>Kz <t t-esc="formatNumber(state.metrics.moneyPerHour, 0)"/></h2>
                  <p class="card-text">
                    <t t-set="prev"  t-value="state.metrics.prevMoneyPerHour"/>
                    <t t-set="curr"  t-value="state.metrics.moneyPerHour"/>
                    <t t-set="delta" t-value="prev ? ((curr - prev)/prev*100) : 0"/>
                    <t t-if="delta >= 0">
                      <span class="text-success">&#9650; <t t-esc="delta.toFixed(1)"/>%</span>
                    </t>
                    <t t-else="">
                      <span class="text-danger">&#9660; <t t-esc="Math.abs(delta).toFixed(1)"/>%</span>
                    </t>
                    &#160;from last period
                  </p>
                </div>
              </div>
            </div>
            <!-- Avg Trips/hr -->
            <div class="col-md-4 mb-3">
              <div class="card text-center h-100">
                <div class="card-body">
                  <h5 class="card-title">Avg Trips / Hour</h5>
                  <h2><t t-esc="formatNumber(state.metrics.tripsPerHour, 2)"/></h2>
                  <p class="card-text">
                    <t t-set="prev"  t-value="state.metrics.prevTripsPerHour"/>
                    <t t-set="curr"  t-value="state.metrics.tripsPerHour"/>
                    <t t-set="delta" t-value="prev ? ((curr - prev)/prev*100) : 0"/>
                    <t t-if="delta >= 0">
                      <span class="text-success">&#9650; <t t-esc="delta.toFixed(1)"/>%</span>
                    </t>
                    <t t-else="">
                      <span class="text-danger">&#9660; <t t-esc="Math.abs(delta).toFixed(1)"/>%</span>
                    </t>
                    &#160;from last period
                  </p>
                </div>
              </div>
            </div>
          </div>
          <!-- third row: Avg Supply Hours per Active Driver -->
          <div class="row mb-4">
            <div class="col-md-4 mb-3">
              <div class="card text-center h-100">
                <div class="card-body">
                  <h5 class="card-title">Avg SH per Active Driver</h5>
                  <h2><t t-esc="formatNumber(state.metrics.supplyHours, 1)"/>h</h2>
                  <p class="card-text">
                    <t t-set="prev"  t-value="state.metrics.prevAvgSupplyHoursPerDriver"/>
                    <t t-set="curr"  t-value="state.metrics.avgSupplyHoursPerDriver"/>
                    <t t-set="delta" t-value="prev ? ((curr - prev)/prev*100) : 0"/>
                    <t t-if="delta >= 0">
                      <span class="text-success">&#9650; <t t-esc="delta.toFixed(1)"/>%</span>
                    </t>
                    <t t-else="">
                      <span class="text-danger">&#9660; <t t-esc="Math.abs(delta).toFixed(1)"/>%</span>
                    </t>
                     from last period
                  </p>
                </div>
              </div>
            </div>
            <!-- Average Utilisation % -->
            <div class="col-md-4 mb-3">
              <div class="card text-center h-100">
                <div class="card-body">
                  <h5 class="card-title">Avg Utilisation</h5>
                  <h2><t t-esc="formatNumber(state.metrics.avgUtilisation, 1)"/>%</h2>
                  <p class="card-text">
                    <t t-set="prev"  t-value="state.metrics.prevAvgUtilisation"/>
                    <t t-set="curr"  t-value="state.metrics.avgUtilisation"/>
                    <t t-set="delta" t-value="prev ? ((curr - prev)/prev*100) : 0"/>
                    <t t-if="delta >= 0">
                      <span class="text-success">&#9650; <t t-esc="delta.toFixed(1)"/>%</span>
                    </t>
                    <t t-else="">
                      <span class="text-danger">&#9660; <t t-esc="Math.abs(delta).toFixed(1)"/>%</span>
                    </t>
                     from last period
                  </p>
                </div>
              </div>
            </div>
            <!-- Average Efficiency % -->
            <div class="col-md-4 mb-3">
              <div class="card text-center h-100">
                <div class="card-body">
                  <h5 class="card-title">Avg Efficiency</h5>
                  <h2><t t-esc="formatNumber(state.metrics.avgEfficiency, 1)"/>%</h2>
                  <p class="card-text">
                    <t t-set="prev"  t-value="state.metrics.prevAvgEfficiency"/>
                    <t t-set="curr"  t-value="state.metrics.avgEfficiency"/>
                    <t t-set="delta" t-value="prev ? ((curr - prev)/prev*100) : 0"/>
                    <t t-if="delta >= 0">
                      <span class="text-success">&#9650; <t t-esc="delta.toFixed(1)"/>%</span>
                    </t>
                    <t t-else="">
                      <span class="text-danger">&#9660; <t t-esc="Math.abs(delta).toFixed(1)"/>%</span>
                    </t>
                     from last period
                  </p>
                </div>
              </div>
            </div>
          </div>          
          <!-- fourth row: Average Acceptance Rate % -->
          <div class="row mb-4">
            <div class="col-md-4 mb-3">
              <div class="card text-center h-100">
                <div class="card-body">
                  <h5 class="card-title">Avg Acceptance Rate</h5>
                  <h2><t t-esc="formatNumber(state.metrics.acceptanceRate, 1)"/>%</h2>
                  <p class="card-text">
                    <t t-set="prev" t-value="state.metrics.prevAcceptanceRate"/>
                    <t t-set="curr" t-value="state.metrics.acceptanceRate"/>
                    <t t-set="delta" t-value="prev ? ((curr - prev)/prev*100) : 0"/>
                    <t t-if="delta >= 0">
                      <span class="text-success">&#9650; <t t-esc="delta.toFixed(1)"/>%</span>
                    </t>
                    <t t-else="">
                      <span class="text-danger">&#9660; <t t-esc="Math.abs(delta).toFixed(1)"/>%</span>
                    </t>
                     from last period
                  </p>
                </div>
              </div>
            </div>
            <!-- Completed to Request % -->
            <div class="col-md-4 mb-3">
              <div class="card text-center h-100">
                <div class="card-body">
                  <h5 class="card-title">Avg Completed to Request</h5>
                  <h2><t t-esc="formatNumber(state.metrics.completedToRequest, 1)"/>%</h2>
                  <p class="card-text">
                    <t t-set="prev" t-value="state.metrics.prevCompletedToRequest"/>
                    <t t-set="curr" t-value="state.metrics.completedToRequest"/>
                    <t t-set="delta" t-value="prev ? ((curr - prev)/prev*100) : 0"/>
                    <t t-if="delta >= 0">
                      <span class="text-success">&#9650; <t t-esc="delta.toFixed(1)"/>%</span>
                    </t>
                    <t t-else="">
                      <span class="text-danger">&#9660; <t t-esc="Math.abs(delta).toFixed(1)"/>%</span>
                    </t>
                     from last period
                  </p>
                </div>
              </div>
            </div>
            <!-- Service Fee -->
            <div class="col-md-4 mb-3">
              <div class="card text-center h-100">
                <div class="card-body">
                  <h5 class="card-title">Service Fee</h5>
                  <h2>Kz <t t-esc="formatNumber(state.metrics.serviceFee, 0)"/></h2>
                  <p class="card-text">
                    <t t-set="prev"  t-value="state.metrics.prevServiceFee"/>
                    <t t-set="curr"  t-value="state.metrics.serviceFee"/>
                    <t t-set="delta" t-value="prev ? ((curr - prev)/prev*100) : 0"/>
                    <t t-if="delta >= 0">
                      <span class="text-success">
                        &#9650; <t t-esc="delta.toFixed(1)"/>%
                      </span>
                    </t>
                    <t t-else="">
                      <span class="text-danger">
                        &#9660; <t t-esc="Math.abs(delta).toFixed(1)"/>%
                      </span>
                    </t>
                    &#160;from last period
                  </p>
                </div>
              </div>
            </div>
          </div>
          <!-- fifth row: Partner Fee -->
          <div class="row mb-4">
            <div class="col-md-4 mb-3">
              <div class="card text-center h-100">
                <div class="card-body">
                  <h5 class="card-title">Partner Fee</h5>
                  <h2>Kz <t t-esc="formatNumber(state.metrics.partnerFee, 0)"/></h2>
                  <p class="card-text">
                    <t t-set="prev"  t-value="state.metrics.prevPartnerFee"/>
                    <t t-set="curr"  t-value="state.metrics.partnerFee"/>
                    <t t-set="delta" t-value="prev ? ((curr - prev)/prev*100) : 0"/>
                    <t t-if="delta >= 0">
                      <span class="text-success">
                        &#9650; <t t-esc="delta.toFixed(1)"/>%
                      </span>
                    </t>
                    <t t-else="">
                      <span class="text-danger">
                        &#9660; <t t-esc="Math.abs(delta).toFixed(1)"/>%
                      </span>
                    </t>
                    &#160;from last period
                  </p>
                </div>
              </div>
            </div>
            <!-- All Drivers -->
            <div class="col-md-4 mb-3">
              <div class="card text-center h-100">
                <div class="card-body">
                  <h5 class="card-title">All Drivers on Yango</h5>
                  <h2><t t-esc="state.allDrivers"/></h2>
                </div>
              </div>
            </div>
          </div>


        </t>

        <!-- LINE CHARTS -->
        <t t-if="state.showCharts">
          <div class="row g-1 mb-1">
            <div class="col-md-4">
              <canvas t-ref="chartActive" style="height:200px"></canvas>
            </div>
            <div class="col-md-4">
              <canvas t-ref="chartTrips"  style="height:200px"></canvas>
            </div>
            <div class="col-md-4">
              <canvas t-ref="chartSupply" style="height:200px"></canvas>
            </div>
            <div class="col-md-4">
              <canvas t-ref="chartCash"   style="height:200px"></canvas>
            </div>
            <div class="col-md-4">
              <canvas t-ref="chartMoneyPerHour" style="height:200px"></canvas>
            </div>
            <div class="col-md-4">
              <canvas t-ref="chartTripsPerHour" style="height:200px"/>
            </div>
            <div class="col-md-4">
              <canvas t-ref="chartAvgSupply" style="height:200px"></canvas>
            </div>
            <div class="col-md-4">
              <canvas t-ref="chartUtilisation" style="height:200px"></canvas>
            </div>
            <div class="col-md-4">
              <canvas t-ref="chartEfficiency" style="height:200px"></canvas>
            </div>
            <div class="col-md-4">
              <canvas t-ref="chartAcceptance" style="height:200px"></canvas>
            </div>
            <div class="col-md-4">
              <canvas t-ref="chartCompletedToRequest" style="height:200px"></canvas>
            </div>
            <div class="col-md-4">
              <canvas t-ref="chartServiceFee" style="height:200px"></canvas>
            </div>
            <div class="col-md-4">
              <canvas t-ref="chartPartnerFee" style="height:200px"></canvas>
            </div>
            <div class="col-md-4 mb-1" style="padding: 0;">
                <canvas t-ref="chartAllDrivers" style="position:absolute; top:0; left:0; width:100%; height:100%;"></canvas>
            </div>
          </div>
        </t>

        <!-- SEARCH + TABLE -->
        <div class="form-group mb-3">
          <input type="text"
                 class="form-control"
                 placeholder="Search driver…"
                 t-model="state.search"/>
        </div>
        <table class="table table-striped table-hover mb-0">
          <thead class="table-light">
            <tr>
              <th style="cursor:pointer" t-on-click="() => setSort('name')">
                Driver
                <t t-if="state.sortKey === 'name'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('quality_score')">
                Quality Score
                <t t-if="state.sortKey === 'quality_score'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('phone')">
                Phone
                <t t-if="state.sortKey === 'phone'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('hire_date')">
                Hire Date
                <t t-if="state.sortKey === 'hire_date'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('product_type')">
                Product
                <t t-if="state.sortKey === 'product_type'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('type')">
                Category
                <t t-if="state.sortKey === 'type'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('active')">
                Active in Period
                <t t-if="state.sortKey === 'active'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('cash')">
                Cash Kz
                <t t-if="state.sortKey === 'cash'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('trips')">
                Trips
                <t t-if="state.sortKey === 'trips'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('hours')">
                Supply Hours
                <t t-if="state.sortKey === 'hours'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('trips_per_hour')">
                Trips/Hour
                <t t-if="state.sortKey === 'trips_per_hour'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('money_per_hour')">
                Money/Hour
                <t t-if="state.sortKey === 'money_per_hour'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('utilisation')">
                Utilisation
                <t t-if="state.sortKey === 'utilisation'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('efficiency')">
                Efficiency
                <t t-if="state.sortKey === 'efficiency'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('acceptance_rate')">
                Acceptance Rate
                <t t-if="state.sortKey === 'acceptance_rate'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('completed_to_request')">
                Completed to Request
                <t t-if="state.sortKey === 'completed_to_request'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('service_fee')">
                Service Fee Kz
                <t t-if="state.sortKey === 'service_fee'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
              <th style="cursor:pointer" t-on-click="() => setSort('partner_fee')">
                Partner Fee Kz
                <t t-if="state.sortKey === 'partner_fee'"><span class="ms-1" t-esc="state.sortDir === 'asc' ? '▲' : '▼'"/></t>
              </th>
            </tr>
          </thead>
          <tbody>
            <t t-foreach="sortedFilteredDrivers()" t-as="d" t-key="d.id">
              <tr>
                <td><t t-esc="d.name"/></td>
                <td t-att-class="qualityClass(d.quality_score)"><t t-esc="d.quality_score"/></td>
                <td><t t-esc="d.phone"/></td>
                <td><t t-esc="d.hire_date"/></td>
                <td><t t-esc="d.product_type"/></td>
                <td><t t-esc="d.type"/></td>
                <td><t t-esc="d.active"/></td>
                <td><t t-esc="formatNumber(d.cash, 0)"/></td>
                <td><t t-esc="formatNumber(d.trips, 0)"/></td>
                <td><t t-esc="formatNumber(d.hours, 0)"/>h</td>
                <td><t t-esc="formatNumber(d.trips_per_hour, 0)"/></td>
                <td><t t-esc="formatNumber(d.money_per_hour, 0)"/></td>
                <td><t t-esc="formatNumber(d.utilisation, 1)"/>%</td>
                <td><t t-esc="formatNumber(d.efficiency, 1)"/>%</td>
                <td><t t-esc="formatNumber(d.acceptance_rate, 1)"/>%</td>
                <td><t t-esc="formatNumber(d.completed_to_request, 1)"/>%</td>
                <td><t t-esc="formatNumber(d.service_fee, 0)"/></td>
                <td><t t-esc="formatNumber(d.partner_fee, 0)"/></td>
              </tr>
            </t>
          </tbody>
        </table>
      </t>
    </div>
  </t>
</templates>
